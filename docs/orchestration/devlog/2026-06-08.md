# 2026-06-08 Devlog

## Expanded Orchestration Interface

- Goal: Update the orchestration standard from the original six-role set to the expanded required core and recommended extensions.
- Changes: Added `STATUS.md`, `RUNBOOK.md`, `SCOPE_GUARD.md`, `README.md`, `devlog/`, and evidence-oriented extension folders to the standard.
- Verification: Markdown files reviewed after editing.
- Problem: The original interface did not clearly separate current state, active task, commands, scope limits, internal logs, and external reports.
- Decision: Treat `reports/` as external-facing output and `devlog/` as the internal black box. Treat AGENTS templates as repository templates first, then promote to a Codex skill after repeated use.
- Next: Implement scanner and dashboard support for detecting this expanded interface.

## Install Command

- Goal: Make the orchestration interface easy to scaffold into another local project.
- Changes: Added `scripts/install-orchestration.mjs`, `npm run orchestration:install`, and default Markdown templates under `docs/orchestration/templates/`.
- Verification: `npm run orchestration:install -- --target . --dry-run` passed.
- Problem: The first dry-run output listed existing directories as created.
- Decision: The installer should skip existing files and folders, and only create missing entries.
- Next: Use this command on real LETHE and SoulLike paths after confirming the target projects.

## Existing Project Migration Prompt

- Goal: Support active projects that already have docs and work history.
- Changes: Added `EXISTING_PROJECT_MIGRATION_PROMPT.md` as an AI handoff prompt for mapping legacy docs into the orchestration interface.
- Verification: Template content reviewed.
- Problem: Empty template installation is not enough for ongoing projects because existing state, decisions, evidence, and scope rules need to be interpreted.
- Decision: Use install command for scaffolding, and use the migration prompt for meaning-preserving conversion.
- Next: Try the prompt on one real project before turning the process into a dashboard action.

## Orchestration Dashboard Status

- Goal: Show the new orchestration interface status in the homepage dashboard.
- Changes: Added scanner checks for required and recommended `docs/orchestration/*` paths, added `files.orchestration` to snapshots, and added an `오케스트레이션 인터페이스` panel in the selected project detail view.
- Verification: `npm run build`; `GET /api/snapshots` returned required core `11/11` for LETHE_Prototype and Project_Orchestrator.
- Problem: Browser plugin visual QA failed with a Windows sandbox spawn error.
- Decision: Treat build and API checks as current verification, and leave browser visual QA as a remaining item.
- Next: Add dashboard actions to copy the install command or existing-project migration prompt.

## Document-First Dashboard

- Goal: Make the homepage show the orchestration documents that drive actual project decisions.
- Changes: Added document content summaries to snapshots, replaced visible risk/prompt/auxiliary scan panels with STATUS/CURRENT_TASK/NEXT_TASKS cards, and added central Discord report preview/send actions.
- Verification: `npm run build`; `GET /api/snapshots`; `POST /api/projects/lethe-prototype/discord-report` dry-run.
- Problem: The previous view still centered scanner signals instead of the orchestration interface.
- Decision: Keep scanner signals in the backend for support, but make the visible operating surface document-first.
- Next: Test one real Discord send after `.env` webhook is confirmed.

## Generated Orchestration Dashboard

- Goal: Create a readable HTML dashboard for the orchestration Markdown interface.
- Changes: Added `scripts/build-orchestration-dashboard.mjs`, `npm run orchestration:dashboard`, and generated `docs/orchestration/index.html`.
- Verification: `npm run orchestration:dashboard`; `npm run orchestration:dashboard -- --all`; checked generated card sections.
- Problem: Markdown is good as source of truth, but too slow to scan manually across projects.
- Decision: Keep Markdown as source and HTML as generated view.
- Next: Add generated indexes for reports, devlog, and evidence.

## Homepage Embedded HTML Dashboard

- Goal: Make generated orchestration HTML visible from the main homepage for each selected project.
- Changes: Added `GET /api/projects/:id/orchestration-dashboard`, embedded it in an `HTML 대시보드` panel, and added a direct `새 창` link.
- Verification: `npm run build`; `GET /api/projects/lethe-prototype/orchestration-dashboard`; homepage HTTP `200 OK`.
- Problem: The running dev server initially used previous server code and returned `Cannot GET` for the new route.
- Decision: Restart only the existing local dev server process and verify the new endpoint with HTTP checks.
- Next: Add a homepage action to regenerate a selected project's orchestration HTML before viewing it.

## Classified HTML Dashboard And Document Browser

- Goal: Make the generated HTML and homepage read like an operating interface instead of a stack of Markdown documents.
- Changes: Removed the top Markdown card row from the homepage, moved current/next instruction summaries into the command panel, regrouped generated HTML into decision categories, and added a `docs/orchestration` file browser.
- Verification: `npm run build`; `npm run orchestration:dashboard -- --all`; LETHE document list, document content, and generated HTML APIs returned `200 OK`.
- Problem: The homepage needed direct document access, but arbitrary path reads would be risky.
- Decision: Restrict document browsing to supported file types inside each project's `docs/orchestration/` directory.
- Next: Add a regenerate button so the selected project's HTML can be refreshed from the homepage.

## Project HTML Interface Split

- Goal: Match the intended interface shape: project dashboard first, then next instruction, command prompt, runbook, and collapsed interface checklist.
- Changes: Generated `index.html`, `command.html`, and `runbook.html`; renamed the embedded dashboard to `프로젝트 대시보드`; removed visible risk badges, the report block, and the document browser from the main flow.
- Verification: `npm run build`; `npm run orchestration:dashboard -- --all`; LETHE dashboard, command, and runbook APIs returned `200 OK`.
- Problem: The previous implementation treated project documents as files to browse instead of interface pages to consume.
- Decision: Treat generated HTML files inside `docs/orchestration/` as the project interface contract, and treat `reports/` as the user-facing progress record.
- Next: Define whether `reports/` is generated per commit, per day, or both.

## LETHE HTML Template And Development Journal

- Goal: Promote the current LETHE HTML format into a reusable orchestration interface template and make report HTML files browsable from the homepage.
- Changes: Added `HTML_INTERFACE_TEMPLATE.md`, updated the migration prompt to reference it, changed the generator toward the LETHE-style dashboard/report-list format, and added a `개발일지` report browser button.
- Verification: `npm run orchestration:dashboard`; `npm run build`; LETHE reports list and selected HTML report APIs returned `200 OK`.
- Problem: The migration prompt had advanced toward `interface/state`, while the current LETHE implementation still uses root-level `index.html`, `command.html`, and `runbook.html`.
- Decision: Preserve LETHE-compatible root layout during adoption unless the user explicitly requests a folder move; treat page roles and format as the stable contract.
- Next: Apply the template to another active project and decide report cadence.
